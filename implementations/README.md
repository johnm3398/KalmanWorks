# KalmanWorks Implementations

This directory groups the language-specific KalmanWorks codebases.

- `python/`: the active Python implementation and packaging root
- `cpp/`: the planned C++ implementation root for a future equivalent codebase

The intent is to keep shared notebooks and repository-level documentation at the top of the
repository, while placing each implementation in its own self-contained project root.
